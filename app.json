{
  "name": "Postgres JDBC",
  "description": "A simple Java app to reproduce a bug in the Postgres JDBC Driver.",
  "addons": [ "heroku-postgresql" ],
  "env": {
    "SOCKS_URL": {
      "value": "http://localhost:9090"
    }
  }
}
